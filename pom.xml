<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.acme.scm</groupId>
    <artifactId>supplychain-backend</artifactId>
    <version>1.0.0-LEGACY</version>
    <packaging>jar</packaging>

    <name>SupplyChain Backend</name>
    <description>Legacy Supply Chain Management System - Backend API</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.1</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-cloud-azure.version>5.22.0</spring-cloud-azure.version>

        <!-- CVE Fixes: Override Spring Boot BOM-managed versions -->
        <!-- GHSA-2m67-wjpj-xhg9, GHSA-6v53-7c9g-w56r, GHSA-72hv-8253-57qq:
             jackson-core 3.x — no BOM artifact available; individual jars overridden in dependencyManagement -->
        <!-- GHSA-24j9-x2wg-9qv6 GHSA-8mc5-53m5-3qj2 GHSA-95jq-rwvf-vjx4 GHSA-9m3c-qcxr-9x87
             GHSA-mgp5-rv84-w37q GHSA-rv64-5gf8-9qq8 GHSA-x4m4-345f-5h5g: Tomcat -->
        <tomcat.version>11.0.21</tomcat.version>
        <!-- GHSA-qqpg-mvqg-649v: logback -->
        <logback.version>1.5.25</logback.version>
        <!-- GHSA-4773-3jfm-qmx3 GHSA-6hcq-hmm3-jj3c: spring-webmvc -->
        <spring-framework.version>7.0.6</spring-framework.version>
        <!-- GHSA-72hv-8253-57qq: jackson-core 2.x (used by azure-core) -->
        <jackson-2-bom.version>2.21.1</jackson-2-bom.version>
        <!-- GHSA-pwqr-wmgm-9rr8: netty-codec-http -->
        <netty.version>4.2.10.Final</netty.version>
        <!-- GHSA-rqfh-9r24-8c9r: assertj-core XXE (test scope) -->
        <assertj.version>3.27.7</assertj.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.azure.spring</groupId>
                <artifactId>spring-cloud-azure-dependencies</artifactId>
                <version>${spring-cloud-azure.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- CVE Fix: GHSA-2m67-wjpj-xhg9, GHSA-6v53-7c9g-w56r, GHSA-72hv-8253-57qq
                 jackson-core 3.x constraint bypass vulnerabilities — fixed in 3.1.1
                 (No Jackson 3.x BOM available; override individual artifacts) -->
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>3.1.1</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>3.1.1</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>3.1.1</version>
            </dependency>

            <!-- CVE Fix: GHSA-xwmg-2g98-w7v9
                 nimbus-jose-jwt DoS via deeply nested JSON — fixed in 10.0.2
                 (No property override available; pin via dependencyManagement) -->
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>10.0.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MySQL Driver -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!-- Azure Identity Extensions for Managed Identity (Azure Database for MySQL) -->
        <dependency>
            <groupId>com.azure</groupId>
            <artifactId>azure-identity-extensions</artifactId>
            <version>1.2.2</version>
        </dependency>

        <!-- Azure Service Bus -->
        <dependency>
            <groupId>com.azure.spring</groupId>
            <artifactId>spring-cloud-azure-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.azure.spring</groupId>
            <artifactId>spring-messaging-azure-servicebus</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ServiceMesh SDK - replaces RestTemplate for inter-service calls -->
        <dependency>
            <groupId>com.acme.mesh</groupId>
            <artifactId>service-mesh</artifactId>
            <version>1.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- InternalLogger SDK - approved logging framework (provides trace ID injection, structured JSON) -->
        <dependency>
            <groupId>com.acme.logging</groupId>
            <artifactId>internal-logger</artifactId>
            <version>1.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
